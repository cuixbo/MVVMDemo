<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="word"
            type="String" />

        <variable
            name="Keywords"
            type="com.cuixbo.mvvm.model.bean.Keywords" />

        <variable
            name="user"
            type="com.cuixbo.mvvm.model.bean.User" />

        <variable
            name="school"
            type="com.cuixbo.mvvm.model.bean.School" />

        <variable
            name="student"
            type="com.cuixbo.mvvm.model.bean.Student" />

        <variable
            name="searchClick"
            type="android.view.View.OnClickListener" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:context=".ui.StateChangeActivity">

        <EditText
            android:id="@+id/et_keyword"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="@={word}"
            app:layout_constraintEnd_toStartOf="@id/tv_search"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="some" />


        <TextView
            android:id="@+id/tv_search"
            android:layout_width="100dp"
            android:layout_height="60dp"
            android:gravity="center"
            android:onClick="@{searchClick}"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="搜索"
            android:textColor="#000"
            app:isVisible="@{true}"
            app:layout_constraintBottom_toBottomOf="@id/et_keyword"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/et_keyword"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_keywords"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:gravity="center"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text='@{user.name.lastIndexOf("9")>10?"9在后":"9在前"}'
            android:textColor="#000"
            app:isGone="@{user.age > 5}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/et_keyword" />


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_keywords"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:padding="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_keywords" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
